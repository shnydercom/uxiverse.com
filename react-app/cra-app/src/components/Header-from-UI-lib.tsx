import React from "react";
import { Header } from "@ldui.net/ui-lib";

export const HeaderFromUILib = () => {
  return (
    <Header
      onLogin={() => {}}
      onCreateAccount={() => {}}
      onLogout={() => {}}
    >
    </Header>
  );
};
