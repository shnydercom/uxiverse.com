import * as React from "react";
import { Header } from "@uxiverse.com/ui-lib";

export const HeaderFromUILib = () => {
  return (
    <Header
      onLogin={() => {}}
      onCreateAccount={() => {}}
      onLogout={() => {}}
    >
    </Header>
  );
};
