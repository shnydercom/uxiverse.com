export const featureFlags = {
  /* this is a feature to position the highlighted element at the end of the siblings (affects treeview) */
  moveTreeHighlightToEnd: false,
  sortTreeViewSiblings: true,
}
